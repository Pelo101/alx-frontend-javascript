/* eslint-disable no-unused-vars */
export default function taskBlock(trueOrFalse) {
  var task = false;
  var task2 = true;

  if (trueOrFalse) {
    var taskBlock = true;
    var taskBlock = false;
  }

  return [task, task2];
}
